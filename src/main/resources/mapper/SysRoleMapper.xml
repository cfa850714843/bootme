<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.phigey.bootme.dao.mapper.SysRoleMapper">

    <resultMap type="com.phigey.bootme.dao.entity.SysRoleEntity" id="sysRoleMap">
        <result property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectSysRoleByUserId"  parameterType="long" resultType="com.phigey.bootme.dao.entity.SysRoleEntity" >
        SELECT `sr`.* FROM `sys_role` AS `sr`
        LEFT JOIN `sys_user_role AS` `se` ON `sr`.`role_id`=`se`.`role_id`
        WHERE `se`.`user_id` = #{userId}
    </select>

</mapper>